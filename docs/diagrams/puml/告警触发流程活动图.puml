@startuml 告警触发流程活动图

start

:系统持续监控传感器数据;

if (数据超出阈值?) then (是)
  :检查告警规则;
  if (符合告警条件?) then (是)
    :触发告警;
    :记录告警日志;
    :通知相关管理员;
    if (管理员确认处理?) then (是)
      :标记告警为已处理;
    else (否)
      :升级告警级别;
    endif
  else (否)
    :继续监控;
  endif
else (否)
  :继续监控;
endif

stop

@enduml